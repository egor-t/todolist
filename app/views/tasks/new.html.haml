%h3 Создаем задачу
= simple_form_for [@list, @task], html: { class: 'form-horizontal' } do |f|
  = f.error_notification
  = f.input :title, placeholder: 'Введите название задачи'
  = f.text_area :description, placeholder: 'Введите описание задачи'
  = f.input :date
  = f.submit 'Создать задачу'